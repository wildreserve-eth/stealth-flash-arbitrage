# 0x Swap API key — get one at https://dashboard.0x.org
# Server-side only: used by the quote fetcher in src/lib/zeroex.functions.ts.
# On Vercel: Project Settings → Environment Variables → add ZEROEX_API_KEY.
ZEROEX_API_KEY=
